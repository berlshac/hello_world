pipeline {
    agent any

    environment {
        NAME = 'user'
    }

    stages {
        stage('Build') {
            steps {
                echo "My name is ${NAME}"
            }
        }
    }
}

